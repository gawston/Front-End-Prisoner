<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('components/utility.ejs') %>
    <title>Document</title>
</head>
<body class="bg-gray-50">
    <!-- nvabar -->
    <%- include('components/navbar.ejs', { userName: 'เข้าสู่ระบบ', orderCount: '12' }) %>
    <div class="container mx-auto text-center py-10 font-semibold text-[#222] text-4xl">
        รายละเอียดการสั่งซื้อ (<span>ORD-3</span>)
    </div>
    <div class="container mx-auto px-4 mb-4">
        <div class="flex flex-col">
            <!-- order details header -->
            <div class="flex items-center bg-white gap-4 py-2 px-4 rounded-[0.375rem_0.375rem_0_0] font-semibold border-t border-x">
                <div class="w-1/2 text-center">สินค้า</div>
                <div class="w-1/6 text-center">ราคา</div>
                <div class="w-1/6 text-center">จำนวน</div>
                <div class="w-1/6 text-center">ราคารวม</div>
            </div>
            <!-- order details items -->
            <% for(let i = 0; i < 12; i++) { %>
                <%- include('components/orderItems.ejs', {
                    productName: 'มาม่ารสต้มยำกุ้ง ' + (i + 1),
                    price: i + 7,
                    amount: i + 1,
                    // total is calculated by price * amount
                    total: (i + 7) * (i + 1),
                    i: i // pass i to identify each item uniquely
                }) %>
            <% } %>

            <!-- all price -->
            <div class="flex items-center bg-white gap-4 py-2 px-4 font-semibold border-t border-x">
                <div class="w-1/2 text-center">รวมทั้งหมด</div>
                <div class="w-1/6 text-center"></div>
                <div class="w-1/6 text-center"><span id="totalAmount" class="text-red-500">12</span></div>
                <div class="w-1/6 text-center">฿ <span id="totalPrice" class="text-red-500">1,000</span></div>
            </div>

            <!-- divider cart items -->
            <div class="w-full py-2 rounded-[0_0_0.375rem_0.375rem] bg-white mb-4 border"></div>

        </div>
    </div>
    <!-- footer -->
    <%- include('components/footer.ejs') %>

    <script src="../javascript/script.js"></script>
</body>
</html>