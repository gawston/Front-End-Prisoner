<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('components/utility.ejs') %>
    <title>Document</title>
</head>
<body class="bg-gray-50">
    <!-- navbar -->
    <%- include('components/navbar.ejs', { orderCount: '12' }) %>
    <div class="container mx-auto text-center py-10 font-semibold text-[#222] text-4xl">
        ข้อมูลส่วนตัว
    </div>

    <form action="/editprofile" method="post" class="container mx-auto p-4 bg-white rounded-md border flex flex-col gap-2 mb-4">
        <div class="flex items-center gap-2">
            <p class="text-xl font-semibold">ชื่อ-นามสกุล:</p>
            <input type="text" name="username" value="<%= user.data.username %>" class="bg-gray-50 border rounded-md px-2 h-10">
        </div>
        <div class="flex items-center gap-2">
            <p class="text-xl font-semibold">เบอร์โทร:</p>
            <input type="text" name="phone" value="<%= user.data.phone %>" class="bg-gray-50 border rounded-md px-2 h-10">
        </div>
        <div class="flex items-center gap-2">
            <p class="text-xl font-semibold">อีเมล:</p>
                <input type="text" name="email" value="<%= user.data.email %>" class="bg-gray-50 border rounded-md px-2 h-10">
        </div>
        <div class="flex items-center gap-2">
            <p class="text-xl font-semibold">รหัสผ่าน:</p>
            <input type="text" name="password" value="<%= user.data.password %>" class="bg-gray-50 border rounded-md px-2 h-10">
        </div>
        <div class="container mx-auto flex justify-center items-center gap-2">
            <input type="submit" class="text-lg font-semibold bg-[#222] text-white hover:text-[#F4BF14] px-4 py-2 rounded-md transition-all" value="บันทึกข้อมูล"></input>
        </div>
    </form>
     
    <script src="../javascript/script.js"></script>

</body>
</html>