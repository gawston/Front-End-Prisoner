<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('components/utility.ejs') %>
    <title>Document</title>
</head>
<body class="bg-gray-50">
    <!-- navbar -->
    <%- include('components/navbar.ejs', { orderCount: '12' }) %>
    <div class="container mx-auto text-center py-10 font-semibold text-[#222] text-4xl">
        ข้อมูลส่วนตัว
    </div>

    <div class="container mx-auto p-4 bg-white rounded-md border flex flex-col gap-2 mb-4">
        <div class="flex items-center gap-4">
            <div class="text-xl font-semibold">ชื่อ-นามสกุล:</div>
            <div class="text-lg"> <%= user.data.username %> </div>
        </div>
        <div class="flex items-center gap-4">
            <p class="text-xl font-semibold">เบอร์โทร:</p>
            <p class="text-lg"> <%= user.data.phone %> </p>
        </div>
        <div class="flex items-center gap-4">
            <p class="text-xl font-semibold">อีเมล:</p>
            <% if(user.data.email != '')  { %>
                <p class="text-lg"> <%= user.data.email %> </p>
            <% } else { %>
                <p class="text-lg"> ไม่มีข้อมูล </p>
            <% } %>
        </div>
        <div class="flex items-center gap-4">
            <p class="text-xl font-semibold">รหัสผ่าน:</p>
            <p class="text-lg"> ******** </p>
        </div>
        <div class="flex items-center gap-4">
            <form action="/deleteuser" method="get">
                <input type="submit" value="ปิดบัญชีผู้ใช้" class="text-lg font-semibold bg-red-500 hover:bg-red-700 text-white px-4 py-2 rounded-md transition-all cursor-pointer" onclick="confirm('คุณต้องการปิดบัญชีใช่หรือไม่')">
            </form>
        </div>
    </div>
    <div class="container mx-auto flex justify-center items-center gap-2 mb-4">
        <a href="/editprofile" class="text-lg font-semibold bg-[#222] text-white hover:text-[#F4BF14] px-4 py-2 rounded-md transition-all cursor-pointer">แก้ไขข้อมูลส่วนตัว</a>
    </div>


    <% if(user.data.role == 'admin') { %>
        <hr/ class="container mx-auto">
        <div class="container mx-auto text-center mt-4 font-semibold text-[#222] text-4xl">
            ผู้ดูแลระบบ
            <div class="container mx-auto p-4 flex flex-col lg:flex-row justify-center items-center gap-2 mt-4">
                <a href="/addproduct" class="text-lg bg-[#222] text-white hover:text-[#F4BF14] rounded-md transition-all px-16 py-2">เพิ่มสินค้าเข้าสู่ระบบ</a>
                <a href="/manageproduct" class="text-lg bg-[#222] text-white hover:text-[#F4BF14] rounded-md transition-all px-16 py-2">จัดการสินค้าในระบบ</a>
                <!-- <a href="/addproduct" class="text-lg bg-[#222] text-white hover:text-[#F4BF14] rounded-md transition-all px-4 py-2"></a> -->
            </div>
        </div>
    <% } %>
</body>
</html>