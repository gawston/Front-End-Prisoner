<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('components/utility.ejs') %>
    <title>Document</title>
</head>
<body>
        <!-- navbar -->
        <%- include('components/navbar.ejs') %>
        <div class="container mx-auto text-center py-10 font-semibold text-[#222] text-4xl">
            แก้ไขสินค้า
        </div>
        <form action="/editproduct/<%= product.productid %>" method="post">
            <input type="text" name="productid" value="<%= product.productid %>" class="hidden">
            <div class="container mx-auto p-4 bg-white rounded-md border flex flex-col gap-2 mb-4">
                <div class="flex items-center gap-2">
                    <p class="text-xl font-semibold">ชื่อสินค้า:</p>
                    <input type="text" name="productname" value="<%= product.productname %>" class="bg-gray-50 border rounded-md px-2 h-10">
                </div>
                <div class="flex items-center gap-2">
                    <p class="text-xl font-semibold">หมวดหมู่:</p>
                    <select name="productcategory" id="category" class="bg-gray-50 border rounded-md px-2 h-10">
                        <option value="foods" <% if(product.productcategory == 'foods') { %> selected <% } %>>อาหาร</option>
                        <option value="drinks" <% if(product.productcategory == 'drinks') { %> selected <% } %>>เครื่องดื่ม</option>
                        <option value="items" <% if(product.productcategory == 'items') { %> selected <% } %>>เครื่องใช้</option>
                        <option value="clothes" <% if(product.productcategory == 'clothes') { %> selected <% } %>>เสื้อผ้า</option>
                        <option value="medicine" <% if(product.productcategory == 'medicine') { %> selected <% } %>>ยาสามัญ</option>
                        <option value="stationery" <% if(product.productcategory == 'stationery') { %> selected <% } %>>เครื่องเขียน</option>
                    </select>
                </div>
                <div class="flex items-center gap-2">
                    <p class="text-xl font-semibold">ราคา:</p>
                    <input type="text" name="productprice" value="<%= product.productprice %>" class="bg-gray-50 border rounded-md px-2 h-10">
                </div>
                <div class="flex items-center gap-2">
                    <p class="text-xl font-semibold">รูปภาพ:</p>
                    <input type="text" name="imgurl" value="<%= product.imgurl %>" class="bg-gray-50 border rounded-md px-2 h-10">
                </div>
            </div>
            <div class="container mx-auto flex justify-center items-center gap-2">
                <input type="submit" class="text-lg font-semibold bg-[#222] text-white hover:text-[#F4BF14] px-4 py-2 rounded-md transition-all cursor-pointer" value="บันทึกข้อมูล"></input>
            </div>
        </form>
        <div class="container mx-auto flex justify-center items-center gap-2">
            <a href="/manageproduct" class="text-lg font-semibold text-[#222] hover:underline mt-4">ย้อนกลับ</a>
        </div>
</body>
</html>